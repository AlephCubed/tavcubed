[package]
name = "tavcubed"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.0-rc.1", features = ["free_camera", "debug"] }
bevy_auto_plugin = "0.11.0-rc.1"
bitflags = "2.11"
bytemuck = "1.25"
crossbeam-channel = "0.5"
image = "0.25"
noiz = "0.5.0-rc.1"
serde = "1.0"
toml = "1.1"

[dev-dependencies]
criterion = "0.8"

[patch.crates-io]
bevy_auto_plugin = { git = "https://github.com/AlephCubed/bevy_auto_plugin", branch = "0.19" }
noiz = { git = "https://github.com/AlephCubed/noiz", branch = "0.19" }

# Idiomatic Bevy code often triggers these lints, and the CI workflow treats them as errors.
# In some cases they may still signal poor code quality however, so consider commenting out these lines.
[lints.clippy]
# Bevy supplies arguments to systems via dependency injection, so it's natural for systems to
# request more than 7 arguments -- which triggers this lint.
too_many_arguments = "allow"
# Queries that access many components may trigger this lint.
type_complexity = "allow"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3

# Remove expensive debug assertions due to <https://github.com/bevyengine/bevy/issues/14291>
[profile.dev.package.wgpu-types]
debug-assertions = false

[profile.release]
# Compile the entire crate as one unit.
# Slows compile times, marginal improvements.
codegen-units = 1
# Do a second optimization pass over the entire program, including dependencies.
# Slows compile times, marginal improvements.
lto = "thin"

[[bench]]
name = "mesh_chunk"
harness = false

[[bench]]
name = "chunk_mutation"
harness = false
